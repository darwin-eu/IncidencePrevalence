# collect_pop_incidence <- function(db,
#                                   results_schema_outcome,
#                                   cohort_ids_outcome = c(1,2,3),
#                                   study_denominator_pops = denominator_pop_dementia,
#                                   cohort_id_denominator_pops = c(1,2,3,4,5,6),
#                                   time_intervals = c("Months", "Years"),
#                                   prior_event_lookbacks = c(5555,0),
#                                   repetitive_events=FALSE,
#                                   confidence_intervals="exact",
#                                   verbose = FALSE){
#
#
#   # tidyr::expand_grid(
#   #   cohort_id_outcome = cohort_ids_outcome,
#   #   cohort_id_denominator_pop = cohort_id_denominator_pops,
#   #   time_interval = time_intervals,
#   #   prior_event_lookback = prior_event_lookbacks,
#   #   repetitive_events = repetitive_events,
#   #   confidence_intervals = confidence_intervals)
#   #
#
# }
